% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/INSiGHT.R
\name{processINSiGHT}
\alias{processINSiGHT}
\title{Run pipeline to process INSiGHT object}
\usage{
processINSiGHT(
  object,
  spot.threshold = 10,
  gene.threshold = 0.05,
  k.NN4bandwidth = 100,
  quantile.bandwidth = 0.1,
  NN4Kernel = FALSE,
  k.NN4Kernel = 100,
  eigen.less = FALSE,
  eigen.prop = 0.3,
  kernel.option = c("single", "mixture"),
  rank.tie = c("min", "max", "first", "last", "random", "average"),
  rank.zero = TRUE,
  weighted = FALSE
)
}
\arguments{
\item{object}{INSiGHT object.}

\item{spot.threshold}{(default 10) filter out the spots whose total expression count
lower than the threshold.}

\item{gene.threshold}{(default 0.05) filter out low-expressed genes
who has a percentage of non-zero expression spots lower than the threshold.}

\item{k.NN4bandwidth}{(default 100) The maximum number of nearest neighbors
to estimate bandwidths.}

\item{quantile.bandwidth}{(default 0.1) A number of probability with values in \eqn{[0,1]},
for computing the quantile of pair-wise distances.
Works only when \code{kernel.option = "single"}.
When \code{kernel.option = "mixture"}, five quantiles (0.1, 0.3, 0.5, 0.7, 0.9) will computed.}

\item{NN4Kernel}{(default FALSE) logical; If true, only the pair-wise distances
of k-nearest-neighbor will be computed for each spot, resulting in a sparse matrix.
If false, all the pair-wise distances will be computed.}

\item{k.NN4Kernel}{The maximum number of nearest neighbors to compute
for the construction of Gaussian kernel using a kd-tree. Works when \code{NN4Kernel == TRUE}.}

\item{eigen.less}{(default FALSE) logical; If true, only the top eigen-values
will be computed with \code{eigs_sym} function to save computing time. Works when \code{NN4Kernel == TRUE}.
If false, all the eigen-values will be computed, which may be time-consuming
when the number of spots is large.}

\item{eigen.prop}{A number between \eqn{[0,1]}
specifying the proportion of top eigen-values to compute.
Works when \code{eigen.less == TRUE}.}

\item{kernel.option}{Charater vector specifying whether a "single" kernel
or a "mixture" of multiple kernels will be contructed for spatial similarity.}

\item{rank.tie}{A character string specifying how ties are treated.
Run \code{?rank} for more details.}

\item{rank.zero}{(default FALSE) logical.
If true, assign zeros a rank of 0 and rank nonzero values from 1 upward.
If false, rank all the values.}

\item{weighted}{(default FALSE)
If true, then the each spatial similarity matrix will be weighted
by its corresponding weight matrix.}
}
\value{
A INSiGHT object.
}
\description{
Process the INSiGHT object before running hypothesis testing, including
quality control process, estimation of bandwidth parameter,
construction of similarity matrices, and assignment of rank scores.
}
